# stucture of code

one module per folder
one class per file
one responsibility per class
one responsibility per function


## Loader (module):
the Loder module has a respoibility to start up the sim, including geting the map and robot. after inisilisation start sim loop.
clas

### MapLoader (class):

## Data:
### MAP(class):
### Robot(class):
### Sim(class):


## SimLoop(module):
### SimManager(class):
the SimManager class is responsible for managing the simulation loop
### SimLoop(class):
#### Step(sim)->sim 
the sim step takes in the curent sim and reterns a new sim.

####
classes: